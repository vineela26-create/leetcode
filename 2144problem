class Solution {
    public int minimumCost(int[] cost) {
        int arr = 0;
      Arrays.sort(cost);
      int n = 0;
      for(int i = cost.length - 1; i >= 0; i--) {
         n++;
         if(n % 3 != 0) {
        arr += cost[i];
       }
     }
     return arr;
    }
}
